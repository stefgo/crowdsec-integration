# The tests work without a running Home Assistant: what is checked is the pure
# logic (metric parser, rates, alert evaluation, time windows) plus the
# consistency of manifest and translations.
pytest>=8.0
pytest-cov>=5.0
aiohttp>=3.9
ruff>=0.8
mypy>=1.13
