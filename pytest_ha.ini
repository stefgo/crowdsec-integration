[pytest]
# The Home-Assistant tests get their own configuration: pytest.ini next door is
# set up for the framework-free suite (importlib mode, coverage over the whole
# component) and neither setting fits here.
testpaths = tests/ha
asyncio_mode = auto
addopts = -q
