[pytest]
testpaths = tests
# Die Testmodule legen sich das Komponentenverzeichnis selbst auf den Pfad;
# ohne importmode=importlib kollidieren gleichnamige Module beim Sammeln.
addopts = -q --import-mode=importlib
